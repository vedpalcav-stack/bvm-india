const router = require("express").Router();
const controller = require("./so.controller");

router.get("/", controller.getSO);
router.post("/", controller.createSO);

module.exports = router;

exports.getSO = async (req, res) => {
  res.json([]);
};

exports.createSO = async (req, res) => {
  res.json({
    success: true,
    message: "Sales Order created"
  });
};
module.exports = {};
module.exports = {};
